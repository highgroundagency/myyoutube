// Phase 1 placeholder. The watched history view is built in the persistence phase.
export function History() {
  return (
    <section aria-labelledby="history-title">
      <h1 id="history-title" className="text-xl font-semibold">
        History
      </h1>
      <p className="mt-2 text-sm text-fg-muted">Videos you have watched will appear here.</p>
    </section>
  );
}
