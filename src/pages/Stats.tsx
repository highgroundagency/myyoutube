// Phase 1 placeholder. The stats dashboard (charts, streak, learning minutes)
// is built in the stats phase.
export function Stats() {
  return (
    <section aria-labelledby="stats-title">
      <h1 id="stats-title" className="text-xl font-semibold">
        Stats
      </h1>
      <p className="mt-2 text-sm text-fg-muted">Your learning time dashboard will appear here.</p>
    </section>
  );
}
